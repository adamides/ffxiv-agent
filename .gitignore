# Secrets — never commit
.env
.env.*
!.env.example

# Local MCP config — hardcodes per-user paths + a service-account key path.
# Ship .mcp.json.example as the genericized template instead.
.mcp.json
!.mcp.json.example

# Community reference PDFs (raid guides etc.) — not ours to redistribute.
# Force-add your own (git add -f) if you author one.
*.pdf

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Fetched log dumps (generated by /ffxivanalyze via scripts/fetch_fight.py)
.fights/
fight*.txt
pdf_text.txt

# OS
Thumbs.db
.DS_Store
